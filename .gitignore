node_modules/
.next/
out/
.DS_Store
*.tsbuildinfo
.env*.local

# Generated test artefacts (produced by scripts/test-*.{js,mjs})
test-*-output.docx
test-debug.docx

# Local diagnostic dumps (DOCX unzipped for manual inspection)
TEMP_UNZIPPED/

# Supabase CLI local state (linked-project ref, pooler url, versions) — per
# machine, not for the repo. Migration .sql files under supabase/migrations
# stay tracked.
supabase/.temp/
supabase/.branches/

# Manual DB backups / dumps — contain real data, never commit.
backup*.sql
*.dump.sql
supabase/backups/

